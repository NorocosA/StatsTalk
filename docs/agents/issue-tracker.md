# Issue Tracker

StatsTalk uses GitHub Issues:
https://github.com/NorocosA/StatsTalk/issues

Create, read, label, comment on, and close issues through the GitHub CLI.
Infer the repository from the configured `origin` remote.

Issues produced by planning skills should be independently implementable,
include acceptance criteria, record blockers, and avoid stale file-level
implementation instructions.
