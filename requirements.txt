# SNLA Core Dependencies
# SPSS Natural Language Assistant — PyWebView Desktop Edition

# Data I/O
pyreadstat>=1.2          # .sav file reading
pandas>=2.0              # .csv reading and data manipulation
numpy>=1.24              # numerical operations

# LLM API
requests>=2.31           # HTTP client for LLM endpoints

# Frontend — Desktop
flask>=3.0               # Embedded HTTP API server
pywebview>=5.0           # Native WebView window (Windows: requires Edge WebView2 runtime)
pythonnet>=3.1           # .NET CLR bridge for pywebview on Windows
clr-loader>=0.2          # CLR host for pythonnet

# Configuration
python-dotenv>=1.0       # .env file loading

# SPSS Output Parsing
lxml>=5.0                # OMS XML parsing

# Report Export
python-docx>=1.0         # Word document generation

# Statistical Engine (Python backend)
pingouin>=0.6.1           # Inferential statistics (t-test, ANOVA, regression, etc.)

# MCP Server (multi-channel frontend)
mcp>=1.24                 # Model Context Protocol Python SDK

# Testing
pytest>=8.0              # Unit/integration testing

# RAG (optional)
# chromadb>=0.4          # Vector database for knowledge base
# sentence-transformers>=2.2  # Embedding model
